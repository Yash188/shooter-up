using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class ScreenBox

{
    public static float Left { get; } = -8f;  // Adjust the left bound as needed
    public static float Right { get; } = 5f;   // Adjust the right bound as needed
    public static float Top { get; } = 3f;     // Adjust the top bound as needed
    public static float Bottom { get; } = -4f; // Adjust the bottom bound as needed
}
